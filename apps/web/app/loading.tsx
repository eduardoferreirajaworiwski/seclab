import { LoadingState } from "@/components/shared/states";

export default function Loading() {
  return <LoadingState title="Loading operator workspace" description="Preparing live program context." />;
}

